// FROM (this looks for the worker in the public root):
// calculationWorkerRef.current = new Worker('/calculation.worker.js');

// TO (this correctly references the co-located worker file):
calculationWorkerRef.current = new Worker(new URL('./calculation.worker.js', import.meta.url));
